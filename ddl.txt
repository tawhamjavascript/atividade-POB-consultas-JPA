CREATE TABLE CARRO (PLACA VARCHAR(255) NOT NULL, ALUGADO BOOLEAN, MODELO VARCHAR(255), PRIMARY KEY (PLACA))
CREATE TABLE ALUGUEL (ID  SERIAL NOT NULL, DATAFIM VARCHAR(255), DATAINICIO VARCHAR(255), DIARIA FLOAT, DIAS INTEGER, FINALIZADO BOOLEAN, VALOR FLOAT, CARRO_PLACA VARCHAR(255), CLIENTE_CPF VARCHAR(255), PRIMARY KEY (ID))
CREATE TABLE CLIENTE (CPF VARCHAR(255) NOT NULL, NOME VARCHAR(255), PRIMARY KEY (CPF))
ALTER TABLE ALUGUEL ADD CONSTRAINT FK_ALUGUEL_CLIENTE_CPF FOREIGN KEY (CLIENTE_CPF) REFERENCES CLIENTE (CPF)
ALTER TABLE ALUGUEL ADD CONSTRAINT FK_ALUGUEL_CARRO_PLACA FOREIGN KEY (CARRO_PLACA) REFERENCES CARRO (PLACA)
